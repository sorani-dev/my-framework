{% extends '@admin/layout.twig' %}

{% block title 'My Posts' %}


{% block content %}
    <h1>Edit the post: {{ item.name }}</h1>
    <form action="{{ path_for('blog.admin.edit', {id: item.id}) }}" method="post">
                {% include "@blog/admin/_form.twig" %}
        <button class="btn btn-primary" type="submit">Edit</button>
    </form>

{% endblock content %}