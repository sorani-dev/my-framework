{% extends '@admin/layout.twig' %}

{% block title 'My Categories - Added' %}


{% block content %}
    <h1>Edit the category: {{ item.name }}</h1>
    <form action="{{ path_for(routePrefix ~ '.edit', {id: item.id}) }}" method="post" class="mb-5">
                {% include viewPath ~ "/_form.twig" %}
        <button class="btn btn-primary" type="submit">Edit</button>
    </form>

{% endblock content %}